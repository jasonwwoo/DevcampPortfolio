<%= form_with(model: blog, local: true) do |form| %>
<form class="" action="index.html" method="post">
  <% if blog.errors.any? %>
    <%= blog.errors.full_messages.each do |error|  %>
      <%=  alert_generator error%>
    <% end %>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control", placeholder: 'Title' %>
  </div>

  <div class="form-group">
    <%= form.label :topic_id %>
    <%= form.collection_select(:topic_id, Topic.all, :id, :title,
                                {
                                  selected: blog.topic_id,
                                  include_blank: true
                                },
                                class: "form-control"
                              )
    %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, class: "form-control", rows: "10", placeholder: 'Content' %>
  </div>

  <div class="form-group">
    <%= form.submit 'Submit', class: "btn btn-primary" %>
  </div>
<% end %>

</form>
<!--
<%= form_tag('/blogs') do %>
  <div class="form-group">
    <%= label_tag 'blog[title]', 'Blog title'%>
    <%= text_field_tag 'blog[title]', blog.title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label_tag 'blog[body]', 'Blog body'%>
    <%= text_area_tag 'blog[body]', blog.body, class: "form-control", rows: 8 %>
  </div>

  <div class="form-group">
    <%= submit_tag 'Save', class: "btn btn-primary" %>
  </div>

<% end%>
 -->
