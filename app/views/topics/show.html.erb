
<div class="col-md-9 blog-main mb-5">
  <h1><%= @topic.title %></h1>
  <% if logged_in?(:site_admin) %>
    <%= link_to 'New Blog', new_blog_path, class: "btn btn-secondary btn-lg mb-4" %>
  <% end %>
  <%= paginate @blogs %>
  <%= render @blogs %>
  <%= paginate @blogs %>


</div>

<!-- /.container -->
